name: Release

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install jq
        run: sudo apt-get update && sudo apt-get install -y jq

      - name: Install deps
        run: bun install

      - name: Build Chrome
        run: bun run build:chrome

      - name: Read version
        id: version
        run: echo "version=$(jq -r .version dist/manifest.json)" >> $GITHUB_OUTPUT

      - name: Zip Chrome
        run: |
          cd dist
          zip -r ../mockify-chrome-${{ steps.version.outputs.version }}.zip .
          cd -

      - name: Restore CRX private key
        shell: bash
        run: |
          echo "${{ secrets.PRIVATE_KEY_BASE64 }}" | base64 -d > private.pem
          chmod 600 private.pem

      - name: Pack CRX
        run: bunx crx3 dist --key private.pem -o mockify.crx

      - name: Build Firefox
        run: bun run build:firefox

      - name: Zip Firefox
        run: |
          cd dist
          zip -r ../mockify-firefox-${{ steps.version.outputs.version }}.zip .
          cd -

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.version.outputs.version }}
          target_commitish: ${{ github.sha }}
          name: Release v${{ steps.version.outputs.version }}
          body: Auto release
          draft: false
          prerelease: false
          generate_release_notes: true
          files: |
            mockify-chrome-${{ steps.version.outputs.version }}.zip
            mockify-firefox-${{ steps.version.outputs.version }}.zip
            mockify.crx
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
